[effect_script*]
{
	name	=lyottia_17_fly_swarm;
	script	=[[
            set $radius .15;

            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(2.185)theta(2.185)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(2.085)theta(2.085)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(1.985)theta(1.985)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(1.885)theta(1.885)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(1.785)theta(1.785)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(1.685)theta(1.685)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(1.585)theta(1.585)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(1.485)theta(1.485)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(1.385)theta(1.385)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(1.285)theta(1.285)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(1.185)theta(1.185)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(1.085)theta(1.085)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(.985)theta(.985)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(.885)theta(.885)";
			sfx start #POP;
			sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(.785)theta(.785)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(.685)theta(.685)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(.585)theta(.585)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(.485)theta(.485)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(.385)theta(.385)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(.285)theta(.285)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(.185)theta(.185)";
			sfx start #POP;
            sfx create orbiter #TARGET
				"must_update()texture(b_sfx_fly)radius($radius)phi(.085)theta(.085)";
			sfx start #POP;
	]];
}