[t:template,n:lyottia_reward_14_safe_passage]
{
	specializes 		= inventory;
	doc 				= "Lyottia reward 14 safe passage";
    category_name       = "generator";
    
	[aspect]
	{
		is_visible = false;
		force_no_render = true;
		is_selectable = false;
	}
    common:is_pcontent_allowed = false;
	[generator_multiple_mp] {}
	[inventory]
	{
		[delayed_pcontent]
        {
            [oneof*]
            {
                il_main = potion_health_super;
                il_main = potion_mana_super;
            }
            [oneof*]
            {
                min = 2;
                max = 3;
                il_main = #weapon/240-290;
                il_main = #armor/470-670;
                il_main = #*/240-290;
            }
            [oneof*]
            {
                il_main = #weapon/-rare(1)/240-290;
                il_main = #armor/-rare(1)/470-670;
                il_main = #*/-rare(1)/240-290;
            }
            [oneof*]
            {
                il_main = #weapon/-unique(2)/240-290;
                il_main = #armor/-unique(2)/470-670;
                il_main = #*/-unique(2)/240-290;
            }
		}
	}
}